<%= provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate render: BootstrapPagination::Rails %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate render: BootstrapPagination::Rails %>